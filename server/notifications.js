import { randomUUID } from 'node:crypto';
export class NotificationService{constructor({notifications=[]}={}){this.notifications=notifications}create({organizationId,userId=null,roles=[],type,title,message,link=null,severity='info',data={}}){const item={id:randomUUID(),organizationId,userId,roles,type,title,message,link,severity,data,createdAt:new Date().toISOString(),readBy:[]};this.notifications.unshift(item);return item}list(actor,{unreadOnly=false}={}){return this.notifications.filter(n=>n.organizationId===actor.organizationId&&(!n.userId||n.userId===actor.id)&&(!n.roles.length||n.roles.includes(actor.role)||actor.role==='admin')&&(!unreadOnly||!n.readBy.includes(actor.id)))}markRead(id,actor){const item=this.notifications.find(n=>n.id===id&&n.organizationId===actor.organizationId);if(!item)return null;if(!item.readBy.includes(actor.id))item.readBy.push(actor.id);return item}markAllRead(actor){const items=this.list(actor);for(const n of items)if(!n.readBy.includes(actor.id))n.readBy.push(actor.id);return items.length}}
